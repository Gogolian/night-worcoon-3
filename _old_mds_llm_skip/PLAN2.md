- popups shoud have backgrounds bright black, and inputs backgound Yellow with text black for visibility.

- Switch or value picker popup (like in boolean values or while activating second config) should display all the buttons available, similar like it was in norton commander ok/cancel it is fine to indicate active button with color, but all buttons should be visible and "button style"

- the adding header bug still persists. i open popup add first header, close popup, and when i open it up again, i see two popups open, i close one and im stuck. It is fine to re-think the whole adding headers mechanics or fix the bug. your call.

- secure=false still displays text editor instead of boolean switch

- plugins config ui should display list of available plugins with on/off switching mechanism like checkboxes
